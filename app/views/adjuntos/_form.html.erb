<%= form_with(model: adjunto, local: true) do |form| %>
  <% if adjunto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(adjunto.errors.count, "error") %> prohibited this adjunto from being saved:</h2>

      <ul>
      <% adjunto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion %>
  </div>

  <div class="field">
    <%= form.label :request_id %>
    <%= form.text_field :request_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
